import { TypeCommand } from '../interfaces/type-command';

export class BotCommand {

    public readonly command: TypeCommand;

    constructor(command: TypeCommand) {
        this.command = command;
    }

    // map wallpaper command
    // map chat command
    // map bot command

}
